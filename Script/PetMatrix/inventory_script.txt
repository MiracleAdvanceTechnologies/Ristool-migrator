SELECT * FROM INV_VENDORITEMS
SELECT * FROM INV_VENDORS WHERE ORG_ID=2
--INSERT INTO INV_VENDORITEMS(ITEM_ID,VENDOR_ID,ORG_ID,IS_ACTIVE)
SELECT  ITEM_ID,15 as VENDOR_ID,2 as ORG_ID,1 as IS_ACTIVE FROM PMS_CHARGABLE WHERE ORG_ID=2
and ITEM_ID NOT IN (SELECT ITEM_ID FROM INV_VENDORITEMS)

UPDATE INV_VENDORITEMS SET VENDOR_ID=15 WHERE ORG_ID=2
UPDATE INV_ITEMDTL  SET ITEM_VENDOR=15 WHERE ORG_ID=2

--INSERT INTO INV_ITEMDTL(ITEM_ID,ITEM_COST,ITEM_VENDOR,IN_HAND,ORG_ID,UNIT_ID)
SELECT ITEM_ID, 500 as ITEM_COST,15 as ITEM_VENDOR,50000 as IN_HAND,2 as ORG_ID,1 as UNIT_ID FROM PMS_CHARGABLE WHERE ORG_ID=2
and ITEM_ID NOT IN (SELECT ITEM_ID FROM INV_ITEMDTL)

SELECT * FROM PMS_CHARGABLE C
JOIN INV_ITEMDTL I ON c.ITEM_ID=I.ITEM_ID
JOIN INV_VENDORS V ON I.ITEM_VENDOR =V.VENDOR_ID
JOIN INV_VENDORITEMS M ON I.ITEM_VENDOR=M.VENDOR_ID AND   I.ITEM_ID=M.ITEM_ID 
AND C.ORG_ID=2


UPDATE  INV_VENDORS SET IS_ACTIVE=1

UPDATE INV_ITEMDTL SET UNIT_ID=36 WHERE ORG_ID=2
SELECT * FROM INV_UNIT WHERE ORG_ID=2



SELECT * FROM INV_PO
SELECT * FROM INV_PODTL where ORG_ID=2

SELECT * FROM INV_PRDTL