Nuclues Frontend
--------------------

Login Credential
-------------------

username:    qa.admin@miracle.com
password:    123456



@ after login you will find the list of request .
@ you can serach by date range.
@ if you want to search a specific api you can search it in the search bar by api name.
@ if you want to filter the internal record use the checkbox named internal.
@ use the below paging option to see all the records.
@ if any problem refresh the page.
@ you can search a transaction of a date easily by first filter the data by specific date range 
and then search that api name into search bar.
@ you can use  these api names for searching

	    HosAppGetAccessToken,
            KKUGetAccessToken,
            VerifyForSignup,
            SentHostAppAccount,
            Kiosk_GetPaymentDetail,
            Kiosk_GetPaymentList,
            Kiosk_Authentication,
            Kiosk_GetPatientProfile,
            VerifyPayment,
            UnlockPayment,
            SetPayment,
            GetDrugAllergy,
            GetLastPrescription,
            GetDrugHistory,
            GetDrugImage,
            GetAppointmentList,
            GetAppointmentDetail,
            CheckInStatus,
            ReceiveQueueNotification,
            ReceiveAppointmentNotification,
            GetQueueInfo,
            ReceivePaymentNotification,
            RecieveVoidPayment,
            PrepareInvoice,
            RecievePrepareNotiComplete,
            SendQrDetailsToHosP,
            ReceivePaidResponse